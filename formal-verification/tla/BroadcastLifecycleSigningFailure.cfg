\* Finding configuration (EXPECTED TO FAIL - see REPORT.md "Findings").
\*
\* With terminal signing failure enabled (reachable only via historical-key
\* signing requests), the on_signature_ready Err arm removes a broadcast from
\* PendingBroadcasts without re-evaluating BroadcastBarriers. If the dropped
\* broadcast was the last pending id at-or-before a barrier, the barrier is
\* never popped and all later broadcasts are blocked forever.
\*
\* TLC is expected to report a violation of BarrierBacked in this config.
SPECIFICATION Spec

CONSTANTS
    Validators = {v1, v2}
    MaxBroadcasts = 3
    RotationIds = {2}
    AllowSigningFailure = TRUE

INVARIANTS
    TypeOK
    BarrierBacked
