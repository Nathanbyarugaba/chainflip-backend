\* Finding config (EXPECTED TO FAIL UserCreditBoundedByDeposit /
\* NoDoubleCredit): when a boosted deposit is full-witnessed at a different
\* amount, the code credits the user again without clearing the boost
\* (BoostNotConsumed path in process_full_witness_deposit_inner).
SPECIFICATION Spec

CONSTANTS
    MaxDeposits = 1
    MaxAmount = 3
    AllowAmountMismatch = TRUE

INVARIANTS
    TypeOK
    BoosterConservation
    LockedCoversOpenBoosts
    NoDoubleCredit
