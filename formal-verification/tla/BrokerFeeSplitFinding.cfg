\* Finding config (EXPECTED TO FAIL NoOverchargeValidated when evaluated
\* only on validated=FALSE states — we check a dedicated invariant here).
\*
\* With AllowUnvalidated, equal 100% splits are included; for amount=3 and
\* 4×2500 bps, Σ fees = 4 > 3. take_broker_fees would assert!-panic after
\* crediting brokers.
SPECIFICATION Spec

CONSTANTS
    MaxAmount = 50
    MaxBeneficiaries = 6
    MaxBps = 1000
    AllowUnvalidated = TRUE

INVARIANTS
    NoOverchargeOnAllModes
